apiVersion: v1
kind: Service
metadata:
  name: booking-ui-service 
  labels:
    app: booking-ui
spec:
  type: LoadBalancer 
  selector:
    app: booking-ui 
  ports:
  - name: http
    protocol: TCP
    port: 80 
    targetPort: http 