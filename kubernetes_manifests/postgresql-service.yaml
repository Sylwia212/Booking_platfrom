apiVersion: v1
kind: Service
metadata:
  name: postgresql-service 
  labels:
    app: postgresql       
spec:
  type: ClusterIP         
  ports:
  - port: 5432            
    targetPort: pgsql     

    protocol: TCP
    name: pgsql          
  selector:
    app: postgresql       