apiVersion: v1
kind: Service
metadata:
  name: booking-api 
  labels:
    app: booking-api 
spec:
  type: ClusterIP 
  selector:
    app: booking-api 
  ports:
  - name: http 
    protocol: TCP
    port: 3000       
    targetPort: http 